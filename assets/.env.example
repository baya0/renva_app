# Copy this file to assets/.env and fill in the values before running the app.
# assets/.env is gitignored (it holds environment-specific configuration).

# Base URL of the Renva backend API, e.g. https://renva.nu/api
API_BASE_URL=
